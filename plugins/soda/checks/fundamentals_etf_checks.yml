checks for fundamentals:
  - row_count > 0
  - duplicate_count(code) = 0
  - missing_percent(etf_total_assets) < 5
  - missing_percent(etf_holdings_count) < 5
  - missing_percent(etf_expense_ratio) < 5
  - missing_percent(etf_yield) < 5

  - avg(etf_total_assets) > 0
  - avg(etf_holdings_count) > 0
  - avg(etf_expense_ratio) >= 0